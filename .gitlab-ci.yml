pages:
  stage: deploy
  script:
  - mkdir .public
  - cp -r * .public
  - mv .public public
  environment:
    name: staging
    url: https://staging.example.com
  artifacts:
    paths:
    - public
  only:
  - master